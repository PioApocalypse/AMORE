== Using the software
To the end user Amore should already be deployed and accessible via local network (LAN).

IMPORTANT: Amore is [underline]#NOT# designed to be exposed to the internet.

NOTE: In Monte S. Angelo's Department of Physics an instance of Amore is deployed on IP address: <{deployed-url}>

